"""Category whitespace tool: deterministic analysis layer.

Computes the structured facts (composition, price deltas, channel math) and
assembles them into an analysis object. All judgment — diagnosis, benchmark
selection, opportunity classification, pack generation, narrative — belongs to
the frontier model via the paste seam or agent orchestration (see README).
"""

__version__ = "0.3.0"
